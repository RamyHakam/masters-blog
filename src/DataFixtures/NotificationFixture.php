<?phpnamespace App\DataFixtures;use App\Factory\AccountFactory;use App\Factory\NotificationFactory;use Doctrine\Bundle\FixturesBundle\Fixture;use Doctrine\Common\DataFixtures\DependentFixtureInterface;use Doctrine\Persistence\ObjectManager;class NotificationFixture extends  Fixture implements  DependentFixtureInterface{    public function load(ObjectManager $manager): void    {        NotificationFactory::new()->many(50)->create(function (){            return [                'owner' => AccountFactory::random()            ];        });        $account = AccountFactory::random()->object();        NotificationFactory::new()->addPost($account )->many(10)->create(function () use ($account){            return [                'owner' => $account            ];        });    }    public function getDependencies()    {       return [           AccountFixture::class,       ];    }}